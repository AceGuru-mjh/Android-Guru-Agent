# 2026-08-11

## CS-Mem PR #33 CI 编译修复记录

分支 `feat/agent-interaction-gaps` 提交 CS-Mem 架构（PR #33），CI 反复编译失败，共四轮修复：

1. `c2d3a05` — K2 不支持局部变量方法引用 `::edgeCounter`（UiTreePruner 改 IntCounter 类）；MemoryGraphStoreImpl FSMMacroEntity 补 `sourceEpisodeId = null`。
2. `372d6bc` — 补 import（AccessibilityManager / width / SkillToolDef）；SkillManifest 当时误删 version。
3. `4f70d77` — SkillManifest 真实有 `version: String` 必填字段，恢复 version；PermissionsScreen openOverlaySettings 改用字面值 `Intent("android.settings.action.MANAGE_APP_OVERLAY_SETTINGS", ...)`，绕开 CI 不可见常量。
4. `de26afc` — SkillScreen.kt:142 `Json{}.encodeToString(manifest)` 类型推断失败（缺 `kotlinx.serialization.encodeToString` 导入，解析到非 reified 重载）。改为 `Json{}.encodeToString(SkillManifest.serializer(), manifest)` 显式传 serializer。✅ CI 通过（run 31491186277）。

### 教训
- 本地无 gradlew/Android SDK，无法端到端编译验证，只能静态核对 + 依赖 GitHub Actions。
- 改领域模型（如 SkillManifest）后必须全局搜索所有调用点。
- 分支操作（checkout/reset/stash）混乱会导致修复 commit 丢失，最终用 `git reset --hard origin/<branch>` + `git cherry-pick` 干净重建。
- `kotlinx.serialization`：在 Compose/屏幕层若未导入顶层 reified `encodeToString`，`Json.encodeToString(instance)` 会解析到非 reified 重载而要求显式 `SerializationStrategy`——显式传 `.serializer()` 最稳。
- 看 CI 失败日志用 `gh run view <id> --log-failed` 重定向到文件再读（PowerShell 无 tail，且 Get-Content 需 -Encoding）。

## PR #34 (feat/consolidate-streaming-tools) CI 编译修复

- 失败：`App Module Compile` 报错 `Unresolved reference 'AlertDialog'` / `'TextButton'`（AgentChatScreen.kt 的 `UserInputDialog` 用到了这两个 Compose 组件但未 import）。
- 修复：`0dba359` 补 `import androidx.compose.material3.AlertDialog` + `.TextButton`；`Button` 在代码中用了全限定名 `androidx.compose.material3.Button`，无需补。
- 关键背景：`origin/main` 在我推送 #34 后从 `2422eb6` 推进到 `4458bfe`（#35 feat/dynamic-slash-menu 已合并）。#34 已 rebase 到新 main（无冲突），clean diff 仅 7 文件 +378。force-with-lease 推送 `27b3f70 -> c4e18ee`。
- 模式：本次 #34 与 #35 同样踩了"新增 Compose 组件未补 import"的坑。教训：**新增 `AlertDialog`/`TextButton`/`Button`/`Icons.*` 等 Compose 符号后，必须同步补对应 `androidx.compose.material3.*` import，否则 CI 才报、本地又无 SDK 验证。**

## Agent 输出 UI 精致化升级（同分支）

审查发现 agent 输出 UI 原始骨架完整，但工具调用类型不分、错误处理太弱。本次全面补齐：

- `AgentChatViewModel.kt`：新增 `enum class ToolKind { LOCAL, MCP, WEB_SEARCH, WEB_FETCH, SKILL }`、`fun classifyTool(toolName, args)`（用前缀 `mcp_call`/`web_search`/`web_fetch` + `skill` 关键字推断，MCP 用正则从 args 取 `server`，不引 kotlinx.serialization 避免模块依赖）、`AgentUiMessage.Error(message, canRetry)` 类型、`retry(text, attachments)` 方法、`skillContext` 字段（Slash Skill 路由时设置、job 完成后清空）。
- `SlashCommandRouter.kt`：`SlashCommandRoute` 加 `skillName` 字段，Skill 分支携带 `command.id`。
- `AgentChatScreen.kt`：新增 `ErrorBlock`（红色 `errorContainer` 卡 + `drawBehind` 描边 + 可选重试）、`ToolKindBadge`（图标+来源标签）、`toolKindStyle`、`WebSearchResultsCard`（解析 WebSearchTool 文本输出为标题/域名/摘要/外链卡片）、`PlainOutputBlock`。`ToolCallCard`/`RunningToolCallCard` 重写：圆形类型图标底 + 状态徽章（完成/失败/运行）+ 来源标签（MCP·server / Skill:xxx）+ 脉冲运行环。
- 图标全部用 `material-icons-extended`（已依赖）：Hub(MCP)、Search、Language(Web)、AutoAwesome(Skill)、Build(本地)、ErrorOutline/WarningAmber、OpenInNew/Link。

**验证说明**：本机仍无 gradlew/Android SDK，无法 `assembleDebug`。已通过 IDE lint（0 错误）+ 静态符号/类型复核。交付前明确告知用户需在真机/CI 跑编译确认。

## 依据 CS-Mem 审计报告落地 P0/P1 修复

用户贴了一份对 `platform/cs-mem` 的代码审计报告（真实性已逐条核对，95%+ 准确）。据此落地了外科手术式修复，**未新建任何并行 package**，全部贴合现有 `platform/cs-mem` 架构：

- **P0-1**：`ApexApp.onCreate()` 注入并启动 `MemoryWriterActor.start()` + `DreamRenderer.scheduleDreamRendering()`（此前 CS-Mem 后台管道从未初始化）。
- **P0-2**：`DreamWorker` 改为 `@HiltWorker` + `@AssistedInject`，`doWork()` 真正调用 `performDreamCycle()`（此前是 `Result.success()` 占位）。依赖 `hilt.work` 已在 build.gradle 配置。
- **P0-3**：`ApexAgentEngine` 系统提示移除已删除的 `memorize`/`recall` 指令（一致性 bug）。
- **P1-1**：`EdgeEntity.toDomain` 改为 suspend `toDomainWithFingerprints()`()，经 node id 反查补全 source/target fingerprint（此前为空字符串，图无法还原）。
- **P1-2**：`ingestDelta` 的 movedNodes 调新增 `NodeDao.updateBounds()` 持久化新坐标（此前只 updateLastSeen，空间记忆漂移）。
- **P1-3**：`removedFingerprints` 调新增 `NodeDao.tombstone()` 做软删除（压低能量 + 刷新 lastSeen，交由低能剪枝回收，不硬删）。
- **P1-4**：`ingestDelta` 整段包 `db.withTransaction` 原子写入；`MemoryWriterActor.flushBatch` 增加单事件级重试（3 次）+ 结构化日志，取代仅 `printStackTrace`。

涉及文件：`ApexApp.kt`、`DreamRenderer.kt`、`ApexAgentEngine.kt`、`MemoryGraphStoreImpl.kt`、`NodeDao.kt`、`MemoryWriterActor.kt`。IDE lint 全 0 错误；仍需真机/CI 编译确认（本机无 SDK）。

**未触碰（报告更高优先级但属架构整合，需另议）**：P2 把 CsMemSessionManager 接入 Agent 主循环、P3 Bypass 接入决策、注册 CS-Mem 召回工具。这些需要改 `AgentModule`/`ToolModule` 注入链路，建议作为下一步里程碑。
