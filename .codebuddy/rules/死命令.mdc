---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-08-10T13:00:43.665Z
provider: 
---

写之前先读代码库 (Read Before You Write)：这是最大的错误来源。在写代码前，必须阅读要修改的文件、了解项目中的类似模式、检查文件顶部的导入、查看测试文件。

想清楚再动手 (Think Before You Code)：与四原则版的第一条类似，强调陈述你的假设和指出权衡，避免因猜测而浪费时间。

保持简单 (Keep It Simple)：与四原则版的第二条类似。

外科手术式修改 (Make Surgical Changes)：与四原则版的第三条类似。

先验证再交付 (Verify Before You Deliver)：在标记任务完成前，运行测试。永远不要在没看到测试通过前就说“完成了”。

目标驱动执行 (Let Goals Drive)：与四原则版的第四条类似。

不猜先调查 (Don't Guess, Investigate)：遇到错误信息或不熟悉的代码时，去读源码或文档，而不是凭空猜测如何修复。

谨慎加依赖 (Be Careful with Dependencies)：添加新依赖前，先看看项目是否已有类似功能的库。新依赖意味着安全风险、体积增加和构建时间变长。

把沟通写清楚 (Write Clearly)：提交信息、PR描述、代码注释都要写清楚。写给人看，而不只是给机器看。新增规则：测试要验证“意图”：有一个很关键的观点是，测试不仅要验证“行为”，更要验证“意图”。这意味着，一个合格的测试用例应该在业务逻辑发生改变时失败，而不仅仅是在代码实现发生变化时失败，从而确保测试真正覆盖了核心功能。

新增规则：坚守原则，适当反驳：当用户的指令或判断违背了基本原则时，AI应该有能力陈述不同意见、提供证据并提出替代方案。这能有效防止为了迎合用户而写出有问题的代码。

新增规则：规则冲突时，明确优先级：当不同规则发生冲突时，需要有一套清晰的仲裁机制。例如，可以将优先级设定为：用户的明确指示 > 反模式（过往教训） > 代码风格规范。

新增规则：从修正中学习：每次用户纠正AI的错误时，AI都应该从中吸取教训，并将这个教训内化，避免在未来重蹈覆辙。这能让AI在长期协作中不断进化。

新增规则：识别并避免失败模式：AI不仅要遵循规则，更要有能力识别自己正在进入哪种典型的失败模式。这种“元认知”能力能帮助它在犯错前就及时刹车或调整策略。